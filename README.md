# Scraping com python
Criar ambiente virtual
- ``` python -m venv venv ```

Instalar o framework Flask
- ``` pip install flask ```

Instalar a biblioteca Selenium
- ``` pip install selenium ```

Iniciar a aplicação
- ``` python app.py ```
